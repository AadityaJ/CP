//http://www.spoj.com/problems/MSCHED/
#include <iostream>
#include <utility>

using namespace std;
int main(int argc, char const *argv[])
{
	int n;
	cin>>n;
	pair <int,int> act[1000];
	for(int i=0;i<n;i++)
		cin>>act[i].second>>act[i].first;
	sort(act,act+n);
	//for(int i=0;i<n;i++)
	//	cout<<act[i].second<<"\t"<<act[i].first<<endl;
	int i=0,t=act[0].second,max;
	while(act[i++].second==t)
	int count_milk = act[0].
	for(int )
	return 0;
}