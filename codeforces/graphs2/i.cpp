#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#define MAX 4010
using namespace std;
int G[MAX][MAX];
int size[MAX];
int main(int argc, char const *argv[]) {
	int n;
	cin>>n;
	string str[n+1];
	for(int i=1;i<=n;i++) cin>>str[i];
	std::vector<int> G[100];
	for(int i=0;i<n-1;i++){
		G[str[i][0]-'a']
	}
	return 0;
}
