//http://codeforces.com/problemset/problem/520/B
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <queue>
#define MAX 10005
using namespace std;
int main(int argc, char const *argv[]) {
	int n,m;
	cin>>n>>m;
	std::vector<int> d(MAX,-1);
	queue <int> q;
	d[n]=0;
	q.enqueue(n);
	while(!q.empty()){
		
	}
	return 0;
}
